{
  "name": "knevee",
  "version": "4.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reggi/knevee.git"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "bin": {
    "knevee": "dist/bin.cjs"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "scripts": {
    "generate": "./src/build/index.ts",
    "build": "npm run generate && tsup --clean ./src/index.ts ./src/bin.ts --format esm,cjs --dts",
    "report": "open ./coverage/index.html",
    "style": "prettier --check .",
    "style:fix": "prettier --write .",
    "test": "npm run generate && mcr --import tsx tsx --experimental-test-snapshots --test",
    "test:snap": "npm run test -- --test-update-snapshots"
  },
  "prettier": "@github/prettier-config",
  "dependencies": {
    "which": "^5.0.0"
  },
  "devDependencies": {
    "@github/prettier-config": "^0.0.6",
    "@types/node": "^22.9.0",
    "@types/which": "^3.0.4",
    "mock-fs": "^5.4.1",
    "monocart-coverage-reports": "^2.11.2",
    "prettier": "^3.2.5",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
